# Music-Audio-Tagging-Content-Understanding
A deep learning system that predicts music attributes (genre, mood, instrumentation) from audio signals and produces embeddings that can be used as features for downstream recommendation systems.
